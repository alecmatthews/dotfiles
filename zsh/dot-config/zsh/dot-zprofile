# .zprofile is sorced for all shells even non-interactive ones

# OS specific configs
if [[ $(uname) == "Darwin" ]]; then
  source "${ZDOTDIR}/os/mac.zprofile"
fi

# Cargo (Rust)
source "${HOME}/.cargo/env" 2>/dev/null || :

# Haskell environment
source "${HOME}/.ghcup/env" 2>/dev/null || :

